export function UploadPage() {
  return (
    <section className="page-panel" aria-labelledby="upload-title">
      <p className="eyebrow">Upload</p>
      <h1 id="upload-title">Upload audio</h1>
      <p className="page-copy">
        Browser upload controls will be added after the shared API client and
        session flow exist.
      </p>
      <div className="empty-state">Upload form placeholder</div>
    </section>
  );
}
